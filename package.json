{
  "name": "Oscilloscope",
  "version": "0.1.0",
  "description": "",
  "main": "index.js",
  "browser": "index.coffee",
  "dependencies": {
    "debug": "^2.1.1"
  },
  "devDependencies": {
    "coffeeify": "^1.0.0",
    "uglifyify": "^3.0.1"
  },
  "browserify": {
    "transform": [ "coffeeify" ]
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "browserify -g [uglifyify --no-sourcemap] example/main.js -o example/bundle.js",
    "watch": "watchify example/main.js -o example/bundle.js --debug --verbose",
    "prepublish": "browserify -s Oscilloscope -g [uglifyify --no-sourcemap] . -o oscilloscope.min.js"
  },
  "keywords": [
    "oscilloscope",
    "waveform",
    "visualization",
    "frequency",
    "AudioNode",
    "MediaStream"
  ],
  "author": "Mathias Rasmussen",
  "license": "MIT"
}
